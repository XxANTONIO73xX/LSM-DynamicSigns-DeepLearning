# LSM-DynamicSigns-DeepLearning
This repository contains the implementation of a deep learning approach for the automatic recognition of dynamic signs in Mexican Sign Language (LSM). It includes a experiments with neural network architectures, highlighting the performance of ResNet-based models.
